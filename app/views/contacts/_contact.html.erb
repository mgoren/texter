<p>
  <% unless contact.name == "" %>
    <%= link_to contact.name, user_contact_path(contact.user, contact) %>
  <% else %>
    <%= link_to contact.phone, user_contact_path(contact.user, contact) %>
  <% end %>
</p>
